name: Deploy to Surge

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Build and Deploy
      run: |
        # Configurar Surge.sh
        npm install --global surge
        surge token <437f459005c9a07d8425c0f90e88f17f> # Reemplazar <SURGE_API_KEY> con la API Key de Surge.

        # Desplegar en Surge.sh
        surge --project ./ --domain <Elec2-Partial1.surge.sh>.surge.sh # Reemplazar <SURGE_SUBDOMAIN> con mi subdominio único 
